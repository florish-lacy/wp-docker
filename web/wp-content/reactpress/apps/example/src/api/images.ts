// Simple Local Avatars plugin

// import { wp } from "./wpapi"

// export async function uploadImage({ file, title, alt_text }: { file: File, title: string, alt_text: string }) {
// 	try {
// 		const result = await wp.media().file(file).create({
// 			title,
// 			alt_text,
// 		})
// 		return result
// 	} catch (error) {
// 		console.error(error)
// 	}
// }
